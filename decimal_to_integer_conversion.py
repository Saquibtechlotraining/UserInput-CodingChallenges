# Write a Python program that asks the user to enter a decimal number,
# and then rounds the number to the nearest integer and prints out the result:-

decimal_no = float(input("Enter the decimal number:"))
print(round(decimal_no))